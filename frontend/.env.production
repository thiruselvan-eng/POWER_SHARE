# Production environment variables for Vercel deployment
# This file is committed to git so Vercel picks it up during build.
# VITE_ prefix is required for Vite to expose the variable to the browser bundle.
VITE_API_URL=https://powershare-backend-m9gz.onrender.com/api
